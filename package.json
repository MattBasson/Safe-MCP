{
  "name": "safe-mcp",
  "version": "0.0.0",
  "private": true,
  "description": "A monorepo of enterprise-safe Model Context Protocol (MCP) servers.",
  "license": "MIT",
  "author": "Matthew Basson",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test"
  }
}
